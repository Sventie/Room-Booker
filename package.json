{
  "name": "room-booker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "setup": "npm install && npm install --prefix backend && npm install --prefix frontend && npm run db:init --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
